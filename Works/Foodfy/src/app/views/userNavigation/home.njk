{% extends "layout.njk" %}

{% block head %}
    <title>Foodfy: home</title>
{% endblock %}

{% block content %}
    <div class="board">
        <div class="board-text">
            <h1>As melhores receitas</h1>
            <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
        </div>

        <div>
            <img src="/assets/chef.png" alt="chef">
        </div>
    </div>

    <p id="mostAccessed">Mais acessadas</p>

    <div class="cards">
        {% for i in range(0, 6) %}
            <div class="card">
                <a href="/recipeslist/{{ recipes[i].id }}">
                    <img src="{{ recipes[i].image_url }}" alt="Imagem de {{ recipes[i].title }}">
                </a>
                <p class="card-name">{{ recipes[i].title }}</p>
                <p class="card-author">{{ recipes[i].name }}</p>
            </div>
        {% endfor %}
    </div>
{% endblock %}